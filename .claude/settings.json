{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -qE '(packages/core/src/|actions/[^/]+/src/)'; then npx --yes pnpm build && git add -f actions/*/dist/; fi; } 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "Rebuilding dist bundles..."
          }
        ]
      }
    ]
  }
}
