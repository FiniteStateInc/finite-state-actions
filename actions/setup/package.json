{
  "name": "@finite-state/setup",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@finite-state/core": "workspace:*"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
